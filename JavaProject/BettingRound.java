
public class BettingRound 
{
	int minimumBet;
	//list of players in a betting round
	/*
	 * a round of betting consists of a player betting x amount instead of checking. the minimum bet is the blind. 
	 * if a player bets instead of checking then the proceeding players can either raise x or call (match the raise). if a player raises then all other players
	 * have to match the bet (call), raise or fold. the betting round will loop infinetly? intill all players either call, or fold.  
	 */
}
